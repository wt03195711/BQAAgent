<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".debug.DebugTaskReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.agents.bqaagent.DEBUG_TASK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".debug.TaskTriggerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.agents.bqaagent.TASK" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
