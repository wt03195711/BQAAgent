<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="12dp"
        android:paddingVertical="12dp">

        <ImageView
            android:id="@+id/ivPlayBadge"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/bg_play_badge"
            android:contentDescription="Play recording"
            android:padding="7dp"
            android:src="@android:drawable/ic_media_play"
            android:tint="@color/colorBrandOnPrimary"
            tools:ignore="UseAppTint" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvTask"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/colorTextPrimary"
                android:textSize="14dp"
                android:textStyle="bold"
                tools:text="Open WhatsApp and send hi to Mom" />

            <TextView
                android:id="@+id/tvMeta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/colorTextTertiary"
                android:textSize="11dp"
                tools:text="Sep 7, 18:57 · 4m12s · 3 parts · 12.4 MB · completed" />

            <TextView
                android:id="@+id/tvNote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/colorTextTertiary"
                android:textSize="10dp"
                android:visibility="gone"
                tools:text="rec_20260907_185706_lz4k2 · stopped early: recorder died"
                tools:visibility="visible" />

        </LinearLayout>

        <ImageView
            android:id="@+id/btnSaveGallery"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Save to Photos"
            android:padding="6dp"
            android:src="@android:drawable/ic_menu_save"
            android:tint="@color/colorTextSecondary"
            tools:ignore="UseAppTint" />

        <ImageView
            android:id="@+id/btnShare"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Share"
            android:padding="6dp"
            android:src="@android:drawable/ic_menu_share"
            android:tint="@color/colorTextSecondary"
            tools:ignore="UseAppTint" />

        <ImageView
            android:id="@+id/btnDelete"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Delete"
            android:padding="6dp"
            android:src="@android:drawable/ic_menu_delete"
            android:tint="@color/colorTextSecondary"
            tools:ignore="UseAppTint" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/colorBorderBase" />

</LinearLayout>